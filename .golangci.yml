version: 2

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - gocyclo
    - govet
    - errcheck
    - ineffassign
  disable:
    - staticcheck
    - revive

linters-settings:
  gocyclo:
    min-complexity: 10

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
